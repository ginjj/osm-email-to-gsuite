# Environment Variables for OSM Email to GSuite App

# Scheduler Authentication Token
# Used by Streamlit UI to call API scheduler endpoints
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SCHEDULER_AUTH_TOKEN=your-secure-token-here

# Google Cloud Project ID
GCP_PROJECT_ID=peak-sorter-479107-d1

# Cloud Run Service URL (auto-set in production, override for local testing)
# CLOUD_RUN_URL=http://localhost:8080
